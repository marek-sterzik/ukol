<!doctype html>
<html>
    <body>
        <h1>Jednoduchý rozcestník</h1>
        <ul>
            {% for link in links %}
                <li><a href="{{ link.link }}">{{ link.name }}</a></li>
            {% endfor %}
        </ul>
    </body>
</html>
